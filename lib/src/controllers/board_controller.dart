/// [KanbanBoardController] is used to control the behaviour of some operations in [KanbanBoard].
/// operations include adding a [group], adding a [group-item], etc.
class KanbanBoardController {
  void addList(String id, dynamic listData) {}
  void addCard(
    String boardId,
  ) {}
}
